import { FileManager } from "@/components/dashboard/student/FileManager";

export const metadata = { title: "File Manager — EduNexus AI" };

export default function DownloadsPage() {
  return <FileManager />;
}

