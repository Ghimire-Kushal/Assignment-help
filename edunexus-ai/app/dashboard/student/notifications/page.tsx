import { NotificationCenter } from "@/components/dashboard/widgets/NotificationCenter";

export const metadata = { title: "Notifications — EduNexus AI" };

export default function NotificationsPage() {
  return <NotificationCenter />;
}
